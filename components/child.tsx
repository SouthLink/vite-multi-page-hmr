import React, { FC } from 'react';
import { Input } from 'antd'

const SearchBar: FC = ({}) => {
  return (
    <div>
      <Input />
      <p>12311231233</p>
    </div>
  );
};

export default SearchBar;
